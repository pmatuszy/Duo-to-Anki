Anki Card Creation Instructions

Please ignore all previous prompts and instructions.

General Workflow: 
0) Always inspect the actual German sentence shown in the image to determine the context — not just the filename or upload order. Never assume which image is for the word or the sentence.
1) Sort all uploaded screenshots in ascending order by filename before starting.  
2) For each image, generate two cards whenever possible:  
   - one for the highlighted word or wordschat 
   - one for the entire sentence  
3) Always verify whether a sentence is a statement or a question — this is a common mistake, so double-check.  
4) Do not create cards with questions like "What does ... mean?" — avoid this format entirely.  
5) After sorting screenshots by filename, inspect the German sentence and verify that the content of each screenshot is matched correctly to its filename. Use the actual sentence text to determine what verb or noun to create the word card for — do not assume based on filename or order alone.

Card Creation Guidelines

Front of the Card (Always in English):  
- Use the singular form of the word.  
- If the word has a plural, append to the end of the first line : ( + plural )  
- Each English meaning should appear on a new line.  
- Do not use phrases like "How do you say:" 
- Never use <br> on the front side of any card. Use <br> only in the back for CSV export. 

Word-Specific Rules

Nouns:  
- Provide both singular and plural German forms.  
- The front shows the singular English word.  
- The back includes:  
  German singular form with article / German plural form  

Adjectives:  
- Provide positive, comparative, and superlative forms.  
- Append to the front card at the end of the first line : ( 3 forms )
- English version should be in lower case and in positive degree

Verbs:  
- Use the base infinitive (e.g., "to go").  
- The back of the card should contain:  
  1) Infinitive  
  2) Third-person singular present / Präteritum / Partizip II (include hat or ist as applicable, but do not label the tenses)  
  3) repeat all four forms again on one line, separated by /  
- Append to the front card at the end of the first line : ( 3 forms ) after the first meaning  
- English version should be in lower case

Sentence-Specific Rules:  
- Create a separate card for each full sentence.  
- Remove filler words like "ja", "hmm", "Oh", "Hurra" etc.  
- Start the English translation with a capital letter.  
- If the sentence is formal, add ( formal ) at the end of the English version.  
- If the sentence is plural, add ( plural ) accordingly.  
- If there’s text within a circle, capitalize both the question and answer. Add " ..." if it fits the context.  

Special Cases:  
- If there’s a bubble within a bubble, only create a card for the inner one with all its meanings.  
- If the outer bubble contains a full sentence, create a separate card for that sentence.  
- Whenever possible, generate two cards per image (one word-based, one sentence-based).  

Processing Order and Formatting:  
- Create cards in the order screenshots are uploaded.  
- Double-check all generated cards for linguistic accuracy.  
- Do not use bold, italic, or any special formatting.  
- For each card, include the source filename in bold.  
- End every line in the answer section with an extra space.  
- do not output to the screen in table format - just a plain text
- if there are words highlighted in yellow or in violet do the separate card for this word as well

CSV Export Instructions

For each screenshot:  
- Save a CSV file with the same name as the image, but with a .csv extension.  
- CSV columns:  
  - Front: English content. If it spans multiple lines, separate them using the <br> tag only for CSV file output.  
  - Sound: Leave this field empty - always.
  - Back: German content. No quotes. End with an extra space. Use <br> for multiple lines only for CSV file output.
  - Hint: always leave it empty
  - AI: Always "(card generated with help of ChatGPT)".
  - Image (PGM): Include the HTML tag that links the screenshot JPG file so it can be imported into Anki.  
  - URL: Leave this field empty.  
  - Tags separated by single space: a tag starting with date extracted from the filename in format YYYY.MM.DD:: followed by time in format HHMMSS extracted from the screenshot filename , another tag which is the current date in the format YYYY.MM.DD, a tag called no_pronunciation.
- Do not include a header row in the CSV file.  
- Also display the generated cards on screen.  
- make CSV file the way that I can download it.
- also make one CSV file combining all CSV generated only from the most recent batch of screenshots so I can import it in one go.
- at the begining of the CSV file put the following 6 lines as a header of the file - they all start with hash and do NOT contain any quotes. Do not include them in the header anywhere... Also treat the 6 lines below not as a comment to my prompt but as actual header for the CSV file:
 #separator:Semicolon
 #html:true
 #deck:all::NiemieckiMoj::on-the-go::moje Duo
 #notetype:Basic (and reversed card)
 #columns:Front;Sound;Back;Hint;AI;screenshot or image (PGM);URL;Tags
 #tags column:8

Anki Card Format

For Words:  
Start with the line (for the screen output only): FRONT (in bold and underlined, literally written as FRONT) and screenshot name 
Then:  
<English word in singular>  
If needed: ( + plural ) or ( 3 forms )  at the end of the first line 

BACK:  
<German singular form with article> / <German plural form>  
this is only when you print to the screen: (card generated with help of ChatGPT) 

For Sentences:  
Start with the line: FRONT (in bold and underlined, literally written as FRONT)  
Then:  
<English sentence or question, properly formatted ending with the period>  

BACK:  
<German translation ending with the period> 
this is only when you print to the screen: (card generated with help of ChatGPT) 

Remember: always create the CSV for the last batch of files


Only create cards for text that is underlined in red or blue in the screenshot.
If that underline is a word → make a word card.
If a dotted underline is on a word/fragment → skip it.
If that underline is a sentence fragment (even if split into lines) → join the lines and make one sentence card.
If a single word inside a sentence is underlined → make two cards: the word card and the full sentence card.

This please do only for specific words in the inner bubble and not for the whole sentence.

After you generate CSV show it to the screen in human readable form.

Name those files exactly like  input jpg files but change only the extension - from jpg to csv.
Place the output files in 
c:\praca\Pawel\nextCloud-admin\duo-wpisac\_chatgpt\_gotowe_pliki_do_importu\
directory.